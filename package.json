{
  "name": "clientforge-site",
  "version": "1.0.0",
  "private": true,
  "description": "ClientForge marketing site \u2014 static, no bundler. JSX is precompiled ahead of time so the browser never ships a compiler.",
  "scripts": {
    "build": "babel app --out-dir app/build --extensions .jsx --presets @babel/preset-react",
    "watch": "npm run build -- --watch",
    "verify": "npm run build -- --out-dir /tmp/cf-build-check >/dev/null && diff -r -x README.md app/build /tmp/cf-build-check && echo 'IN SYNC: app/build matches the .jsx sources'"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.0",
    "@babel/core": "^7.24.0",
    "@babel/preset-react": "^7.24.0"
  }
}
